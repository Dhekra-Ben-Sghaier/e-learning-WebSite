{% extends 'base_admin.html.twig' %}

{% block title %}InscriCertif{% endblock %}

{% block content %}
    <h1>InscriCertif</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdInscri</th>
                <td>{{ inscri_certif.idInscri }}</td>
            </tr>
            <tr>
                <th>NomUtilisateur</th>
                <td>{{ inscri_certif.nomUtilisateur }}</td>
            </tr>
            <tr>
                <th>NomCertificat</th>
                <td>{{ inscri_certif.nomCertificat }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('inscri_certif_index') }}">back to list</a>

    <a href="{{ path('inscri_certif_edit', {'idInscri': inscri_certif.idInscri}) }}">edit</a>

    {{ include('inscri_certif/_delete_form.html.twig') }}
{% endblock %}
