{% extends 'base.html.twig' %}

{% block title %}Formation{% endblock %}

{% block body %}
    <h1>Formation</h1>

    <table class="table">
        <tbody>
        <tr>
            <th>Id</th>
            <td>{{ formation.id }}</td>
        </tr>
        <tr>
            <th>Titre</th>
            <td>{{ formation.titre }}</td>
        </tr>
        <tr>
            <th>Description</th>
            <td>{{ formation.description }}</td>
        </tr>
        <tr>
            <th>Prix</th>
            <td>{{ formation.prix }}</td>
        </tr>
        <tr>
            <th>Difficulte</th>
            <td>{{ formation.difficulte }}</td>
        </tr>
        <tr>
            <th>Cours</th>
            <td>{{ formation.cours }}</td>
        </tr>
        <tr>
            <th>Image</th>
            <td>{{ formation.image }}</td>
        </tr>
        </tbody>
    </table>

    <button class="btn"><a href="{{ path('apprenant_index') }}">Retourner à la liste des formations</a></button>

    <button class="btn"><a href="{{ path('formation_edit', {'id': formation.id}) }}">Modifier</a></button>


    {{ include('formation/_delete_form.html.twig') }}
{% endblock %}
