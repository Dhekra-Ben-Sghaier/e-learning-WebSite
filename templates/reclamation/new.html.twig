{% extends 'personnes/indexuser.html.twig' %}

{% block title %}New Reclamation{% endblock %}

{% block body %}
    <link href="{{ asset('/admin/css/rec.css') }}" rel="stylesheet">
    {{ form_start(form,{'attr': {'novalidate': 'novalidate'}} ) }}
<div class="row justify-content-center">

    <div class="col-xl-10 col-lg-12 col-md-9">

        <div class="card o-hidden border-0 shadow-lg my-5">
            <div class="card-body p-0">
                <!-- Nested Row within Card Body -->
                <div class="row">
                    <div class="col-lg-6 d-none d-lg-block bg-login-image"></div>
                    <div class="col-lg-6">
                        <div class="p-5">
    <h1>Create new Reclamation</h1>


    </tr>
    <tr class="group">
        <td class="label"> {{form_label(form.adressem,'email')}}</td>
        <td> {{form_widget(form.adressem,{'attr': {'class': 'form-control form-control-user'}})}}</td>
        {{ form_errors( form.adressem) }}
    </tr>

    <tr class="group">
        <td class="label"> {{form_label(form.examen,)}}</td>
        <td> {{form_widget(form.examen,{'attr': {'class': 'form-control form-control-user'}})}}</td>
        {{ form_errors( form.examen) }}

    </tr>
    <tr class="group">
        <td class="label"> {{form_label(form.date)}}</td>
        <td> {{form_widget(form.date,{'attr': {'class': 'form-control form-control-user'}})}}</td>
        {{ form_errors( form.date) }}

    </tr>
    <tr class="group">
        <td class="label"> {{form_label(form.nomFormateur)}}</td>
        <td> {{form_widget(form.nomFormateur,{'attr': {'class': 'form-control form-control-user'}})}}</td>
        {{ form_errors( form.nomFormateur) }}
    </tr>
    <tr class="group">
    <td class="label"> {{form_label(form.description)}}</td>
    <td> {{form_widget(form.description,{'attr': {'class': 'form-control form-control-user'}})}}</td>
        {{ form_errors( form.description) }}
    </tr>
    </table>









    {{ include('reclamation/formulaire.html.twig') }}

    <a href="{{ path('reclamation_index') }}">back to list</a>
                        </div>
                    </div>
                </div>

            </div>
        </div>

    </div>

</div>

    </div>
    {{ form_end(form) }}
{% endblock %}
