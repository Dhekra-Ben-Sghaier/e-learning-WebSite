{% extends 'base_admin.html.twig' %}

{% block title %}Publicite{% endblock %}

{% block content %}


    <h1 align="center" style="color:#E66207">Afficher l'annonce de la publicité</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ publicite.id }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ publicite.nom }}</td>
            </tr>
            <tr>
                <th>Prenom</th>
                <td>{{ publicite.prenom }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ publicite.email }}</td>
            </tr>
            <tr>
                <th>Domaine</th>
                <td>{{ publicite.domaine }}</td>
            </tr>
            <tr>
                <th>Affichage</th>
                <td>{{ publicite.affichage }}</td>
            </tr>
            <tr>
                <th>Image</th>
                <td>{{ publicite.image }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ publicite.prix }}</td>
            </tr>
            <tr>
                <th>Lien</th>
                <td>{{ publicite.lien }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('publicite_index') }}">back to list</a>

    <center><a  href="{{ path('publicite_edit', {'id': publicite.id}) }}" >edit</a></center>

    {{ include('publicite/_delete_form.html.twig') }}
{% endblock %}
