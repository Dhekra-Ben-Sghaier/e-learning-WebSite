<form method="post" action="{{ path('recformation_delete', {'idFormation': recformation.idFormation}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ recformation.idFormation) }}">
    <button class="btn">Delete</button>
</form>
