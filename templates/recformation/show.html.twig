{% extends 'base.html.twig' %}

{% block title %}Recformation{% endblock %}

{% block body %}
    <h1>Recformation</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdFormation</th>
                <td>{{ recformation.idFormation }}</td>
            </tr>
            <tr>
                <th>Adressemail</th>
                <td>{{ recformation.adressemail }}</td>
            </tr>
            <tr>
                <th>Formation</th>
                <td>{{ recformation.formation }}</td>
            </tr>
            <tr>
                <th>NomFormateur</th>
                <td>{{ recformation.nomFormateur }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ recformation.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('recformation_index') }}">back to list</a>

    <a href="{{ path('recformation_edit', {'idFormation': recformation.idFormation}) }}">edit</a>

    {{ include('recformation/_delete_form.html.twig') }}
{% endblock %}
